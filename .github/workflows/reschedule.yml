{"name": "Reschedule soken", "on": "workflow_dispatch", "permissions": {"contents": "write"}, "jobs": {"run": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"uses": "actions/setup-python@v5", "with": {"python-version": "3.12"}}, {"name": "reschedule", "run": "python scripts/reschedule_soken.py"}, {"name": "commit", "run": "git config user.name github-actions && git config user.email actions@users.noreply.github.com && git add posts-scheduled.json && (git diff --staged --quiet && echo nochange || (git commit -m 'chore: reschedule soken to twice weekly' && git push))"}]}}}
