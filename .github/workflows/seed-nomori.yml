{"name": "Seed nomori posts", "on": "workflow_dispatch", "permissions": {"contents": "write"}, "jobs": {"seed": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"uses": "actions/setup-python@v5", "with": {"python-version": "3.12"}}, {"name": "merge", "run": "python scripts/merge_nomori.py"}, {"name": "commit", "run": "git config user.name github-actions && git config user.email actions@users.noreply.github.com && git add posts-scheduled.json && (git diff --staged --quiet && echo nochange || (git commit -m 'chore: seed nomori posts' && git push))"}]}}}
